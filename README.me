This plugin contains functions that help with debugging certain features in Image Source Control.

You only need to uncomment the `add_action` or `add_filter` function you want to run.

Check out the comments in the appropriate functions for further options and adjustments.